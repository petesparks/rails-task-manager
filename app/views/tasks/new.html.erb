<div class="container">
  <div class="row">
    <div class="card card-edit">
      <div class="card-body">
        <%= form_for(@task) do |f| %>
        <%= f.label :title %>
        <%= f.text_field :title %>

        <%= f.label :details %>
        <%= f.text_field :details %>

        <%= f.submit %>
        <% end %>

      </div>
    </div>
  </div>
</div>
